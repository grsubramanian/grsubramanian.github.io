<div class="col">
  <a href="{% if interest.redirect %}{{ interest.redirect }}{% else %}{{ interest.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if interest.img %}
        {%
          include figure.liquid
          loading="eager"
          path=interest.img
          sizes = "250px"
          alt="interest thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ interest.title }}</h2>
        <p class="card-text">{{ interest.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if interest.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ interest.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if interest.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ interest.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
